services:
  - type: web
    name: ai机器人
    env: python
    plan: free  # 选择免费计划
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python Ai机器人.py"
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
